Ledgerplot.

Usage:
    ledgerplot --file=<file_name> --startyear=<year_start> --endyear=<year_end> [--income_vs_expenses|--cashflow|--wealthgrowth|--income_per_category|--expenses_per_category] [--yearly|--quarterly|--monthly|--weekly]
    ledgerplot --help
    ledgerplot --version

Options:
    --file=<file_name>          Ledger dat filename to use.
    --period=<period string>    Plot for this period.
    --income_vs_expenses        Plot income vs expenses.
    --cashflow                  Plot cashflow.
    --wealthgrowth              Plot wealthgrowth.
    --income_per_category       Plot income per category.
    --expenses_per_category     Plot expenses per category.
    --yearly                    Plot totals per year.
    --quarterly                 Plot totals per year, for each quarter.
    --monthly                   Plot totals per month.
    --weekly                    Plot totals per week.
    -h --help                   Show this screen.
    --version                   Show version.
    --verbose                   Display info on stdout.
